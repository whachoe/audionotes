package com.cjpa.notes.ui.theme

import androidx.compose.ui.graphics.Color

// Primary brand color (indigo) and error color (red), per the approved mockup.
val Indigo = Color(0xFF3F51B5)
val IndigoDark = Color(0xFF303F9F)
val IndigoLight = Color(0xFFC5CAE9)
val OnIndigo = Color(0xFFFFFFFF)

val Red = Color(0xFFD32F2F)
val RedDark = Color(0xFFB71C1C)
val OnRed = Color(0xFFFFFFFF)

// Status chip colors: one distinct hue per status.
val StatusOpenColor = Color(0xFF1976D2)
val StatusInProgressColor = Color(0xFFF9A825)
val StatusTodoColor = Color(0xFF7B1FA2)
val StatusClosedColor = Color(0xFF388E3C)

// Copywaste brand colors, sampled from the copyWaste wordmark at copywaste.org
// - used for the "Copywaste Notes" wordmark lockup title.
val CopywasteNavy = Color(0xFF09136C)
val CopywasteRed = Color(0xFFC40606)

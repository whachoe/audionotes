<?xml version="1.0" encoding="utf-8"?>
<!-- Round (oval) background for the idle-state Glance widget button, so the
     button renders as a small round icon-only control rather than a rectangle. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/indigo_primary" />
</shape>
